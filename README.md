This is simple template react + typescript + eslint(airbnb) + husky

Example code in todo branch

yarn

yarn start

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).
